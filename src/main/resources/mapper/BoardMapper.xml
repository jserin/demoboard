<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.demoBoard.mapper.BoardMapper">
    <resultMap id="boardResultMap" type="demo.demoBoard.model.BoardRequest">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="create_date" column="create_date"/>
    </resultMap>

    <select id="getAllBoards" resultMap="boardResultMap">
        SELECT * FROM board
    </select>

    <insert id="insertBoard" parameterType="demo.demoBoard.model.BoardRequest">
        INSERT INTO board (title, content, create_date)
        VALUES (#{title}, #{content}, SYSDATE)
    </insert>
</mapper>